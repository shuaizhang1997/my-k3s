apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-collector
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otel-collector
  template:
    metadata:
      labels:
        app: otel-collector
    spec:
      containers:
      - name: otel-collector
        image: otel/opentelemetry-collector-contrib:sha256-4e8508c4fc97b221d4b30288b1a48707da20ca010f32443af21769502edec2f0.sig
        env:
        - name: CH_PASSWORD
          valueFrom:
            secretKeyRef:
              name: clickhouse-auth
              key: password
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
        args: ["--config=/etc/otel-collector/relay.yaml"]
        volumeMounts:
        - name: otel-collector-config-vol
          mountPath: /etc/otel-collector/relay.yaml
          subPath: relay.yaml
      volumes:
      - name: otel-collector-config-vol
        configMap:
          name: otel-collector-conf
